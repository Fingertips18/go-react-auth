package constants

const (
	ROOT = "/"
	AUTH = "/api/auth"
)

const (
	SIGNUP         = "/sign-up"
	SIGNIN         = "/sign-in"
	SIGNOUT        = "/sign-out"
	VERIFY         = "/verify"
	FORGOTPASSWORD = "/forgot-password"
	RESETPASSWORD  = "/reset-password"
)
